# Copiar este archivo a .env para desarrollo local.
# En dev se deja como '/api' porque Vite lo redirige al backend (ver
# vite.config.ts). En producción, apuntar directo a la URL del backend,
# ej: https://api.mi-dominio.com/api
VITE_API_URL=/api
